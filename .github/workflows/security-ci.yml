name: Security CI (Reusable)

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  run-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Run SAST
        uses: saicharank0513-sys/DEV/.github/actions/sast@main

      - name: Run SonarQube Coverage
        uses: saicharank0513-sys/DEV/.github/actions/sonarqube@main

      - name: Run Unit Tests
        uses: saicharank0513-sys/DEV/.github/actions/unittest@main
