name: Security CI (Reusable)

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout caller repo
        uses: actions/checkout@v4

      - name: Run SAST
        uses: saicharank0513-sys/DEV/.github/workflows/security-ci.yml@main

      - name: Run SonarQube Coverage
        uses: saicharank0513-sys/DEV/.github/workflows/security-ci.yml@main

      - name: Run Unit Tests
        uses: saicharank0513-sys/DEV/.github/workflows/security-ci.yml@main
